# Changelog

## 1.0.0 — 2026-07-30

### Added

- 正式版一对一学习契约：成功定义、时间预算、教练方式、反馈偏好、挑战强度、自主性目标、最低承诺与复盘周期；
- `learning_contracts`与`weekly_learning_reviews` Schema V10表和索引；
- 正式周复盘：计划/完成会话、微行动、能力证据、用户反思、教练判断、下周唯一重点与行为调整；
- `OneToOneState`目标级契约状态、自主性差距与下次复盘日期；
- `setup-contract`与`weekly-review`自然语言意图；
- `contract.upsert`与`review.weekly` CHART工具；
- 一对一契约与周复盘Inspector界面；
- 正式版、契约、迁移、发布门禁文档与正式README。

### Changed

- Self-Study由实验性版本线收敛为V1.0长期兼容正式版；
- C5 Context新增当前契约、最近周复盘和目标级自主性状态；
- Alignment要求计划服从真实时间预算，并按自主性差距撤除AI脚手架；
- 首页在契约缺失时优先提示建立契约；
- 周复盘同时检查行为、学习与成果，不把连续天数当作能力；
- Package版本、描述、路线、架构、数据和安全文档更新为正式版。

### Validation

- 69个TypeScript/TSX/配置文件语法检查零错误、相对导入零缺失；
- 核心Main/Shared严格TypeScript语义检查通过；
- 高置信硬编码密钥扫描通过；
- 真实SQLite Schema V10、一对一契约、周复盘、目标级自主性状态与完整性检查通过；
- V0.9真实数据库升级到V1.0，旧目标与消息保留，未自动制造契约；
- 源码清单与ZIP完整性检查通过。

## 0.9.0 — 2026-07-29

### Added

- 福格行为模型工程层：动机、能力、提示、结果和庆祝分离记录；
- 目标级`BehaviorState`与全局行为汇总；
- 微习惯配方：锚点、最小行为、可选扩展、即时庆祝、频率和最小时长；
- `habit_recipes`与`habit_checkins` Schema V9表和索引；
- 自然语言`behavior-diagnose`、`design-habit`和`habit-checkin`意图；
- `behavior.diagnose`、`habit.design`和`habit.checkin` CHART工具；
- 一对一微习惯Inspector面板、行为画像、Check-in和暂停/恢复；
- 首页唯一下一步与微习惯、低能力和提示失效联动；
- 行为设计、迁移和验证文档。

### Changed

- C5 Context优先读取当前Goal的行为画像，避免跨目标污染；
- Alignment加入不羞辱、不补偿、最小版本即成功和连续天数反强迫约束；
- Adaptive Engine在高摩擦状态下优先缩小行动，再推进正式里程碑；
- 自然语言“完成最小行动/打卡”优先路由到Check-in，修复与习惯设计关键词冲突；
- Package描述和路线图更新为一对一行为设计自学系统。

### Validation

- 66个TypeScript/TSX/配置文件语法与相对导入检查零错误；
- Main/Shared/Preload严格语义检查通过；
- 真实SQLite Schema V9、微习惯、Check-in、连续记录、目标行为画像和完整性测试通过；
- V0.8真实数据库升级到V0.9，旧目标与消息保留，新增表和默认行为画像正常；
- 高置信硬编码密钥扫描通过。

## 0.7.0 — 2026-07-27

- 明暗主题、文字倍率、阅读宽度、密度、增强对比和减少动态效果；
- 统一视觉令牌、自然阅读轴、可访问焦点和视觉对比校验；
- Schema v7持久化外观偏好。

## 0.6.0 — 2026-07-27

### Added

- 自然简洁双栏默认布局，右侧进度改为按需抽屉；
- 专注模式，只保留当前目标聊天和输入；
- 每个目标独立持久化未发送草稿；
- 目标会话置顶、重命名、归档、恢复和最近打开排序；
- 自伸缩输入框、Enter发送、Shift+Enter换行；
- 根据里程碑、复习、评估和任务生成情境建议；
- 会话内错误提示、关闭和一键重试；
- Ctrl/Cmd+N、K、逗号和Shift+F快捷键；
- Schema v6会话交互字段与相应类型化IPC；
- V0.6自然交互与持久化自动化测试。

### Changed

- 右侧Inspector默认关闭，避免首次进入信息过载；
- 目标列表按活动状态、置顶和最近打开排序；
- 发送消息后只清空当前目标草稿；
- 归档目标时自动取消置顶，但保留全部学习资产；
- 全局错误Toast改为当前会话内联恢复；
- 日常页面隐藏复杂CHART设置，移入更多菜单。

### Validation

- 61个TypeScript/TSX/配置文件完成语法和相对导入检查；
- Schema v6真实SQLite草稿隔离、置顶、重命名、归档恢复和消息清草稿测试通过；
- V0.5→V0.6迁移保留旧目标、旧会话和旧消息，并补齐四个新字段。

## 0.5.0 — 2026-07-27

### Added

- 克制的三栏桌面布局：目标会话、主聊天、目标Inspector；
- `react-markdown + remark-gfm + remark-math + rehype-katex + KaTeX`富文本链；
- CommonMark/GFM表格、任务列表、代码块复制、行内与块级LaTeX；
- `goal_conversations`目标会话实体；
- Message和AgentRun的`goalId + conversationId`双重关联；
- 新目标临时会话创建后原子迁移到正式目标；
- 每目标消息总数、最后更新时间和最后摘要；
- 每个会话独立加载最近500条，完整记录永久保存在SQLite；
- 全量JSON/Markdown导出，并按目标分组导出会话正文；
- 当前目标聊天搜索与目标列表搜索；
- 新目标Run完成后桌面自动切换到新目标。

### Changed

- 数据库Schema从v4升级至v5；
- Context Engine严格按当前目标编译，不再在无目标时回退到任意活动目标；
- C5 Learner State改为目标级计算；
- Inspector在新目标模式不再混显其他目标数据；
- 无`goalId`的桌面消息强制按新目标意图处理；
- 主窗口默认背景和整体视觉改为白色、灰阶和单一绿色主行动。

### Security

- Markdown不解析原始HTML；
- KaTeX禁用trusted commands并启用严格解析；
- 外部链接继续由Main进程控制；
- 会话和目标一致性在服务层进行校验。

### Validation

- 59个TypeScript/TSX/配置文件语法与相对导入检查通过；
- 高置信密钥扫描通过；
- SQLite Schema v5新库、目标隔离、临时Run迁移和V0.4→V0.5迁移测试通过；
- 每会话读取上限不会使其他目标会话饥饿，全量导出仍保留所有消息。

## 0.4.0 — 2026-07-27

### Added

- “目标→路径→资料→学习→评估→作品验收”的一条龙学习产品链；
- 版本化学习路径与六阶段里程碑状态机；
- 里程碑证据门槛、顺序解锁、完成和整条路径归档/完成；
- 本地资料库，支持TXT、Markdown、JSON、CSV显式导入；
- SHA-256去重、5MB单文件限制、1600字符重叠分块和本地关键词检索；
- 中文二至四字词组检索召回，改善无空格中文查询；
- 资料来源片段进入Context，并在Alignment中区分来源事实、模型推断和建议；
- 独立掌握评估，覆盖机制解释、跨场景迁移、误解与验证设计；
- 评估提交、量化得分、反馈、通过状态和能力证据沉淀；
- 作品量表自动验收、逐项分数、通过/修订状态与验收证据；
- 路径、资料库、评估三个新的内置Agent Skill；
- 路径、资料、评估、作品验收的自然语言意图和Agent计划；
- Path、Library、Assessments桌面Inspector面板；
- JSON/Markdown导出新增路径、资料、评估和作品验收信息；
- SQLite Schema v4与V0.3→V0.4真实迁移演练；
- V0.4端到端SQLite集成测试。

### Changed

- 数据库Schema从v3升级至v4；
- A5不再只表示教学提示，而具有路径、证据门槛和阶段解锁实体；
- C5 Context编译新增路径状态、开放评估、个人资料及相关检索片段；
- B5新增路径编译、评估提交、作品验收等持久化风险规则；
- D5导出和Trace覆盖资料来源、评估尝试及作品验收；
- 创建长期目标后自动编译第一版六阶段学习路径；
- 自适应引擎把当前可用里程碑和待完成评估纳入下一最佳行动；
- “考考我”从普通verify路由升级为独立掌握评估；
- README、架构、数据模型、CHART、校验和路线文档升级到V0.4。

### Security

- 文件导入只能由用户通过系统文件选择器显式授权；
- 拒绝不支持的扩展名、超过5MB的文件和超过20个的批量导入；
- 本地资料不自动上传，只有命中的必要片段可进入一次模型Context；
- 导入内容使用SHA-256校验和去重，不把API密钥写入资料、Trace或导出。

## 0.3.0 — 2026-07-27

- 知识边、误解、间隔复习、会话、作品、能力证据、模型成本、运行韧性和CI。

## 0.2.0 — 2026-07-27

- 九家模型、DeepSeek默认、四协议适配、自动回退、D5跨模型审计和安全密钥控制面。

## 0.1.0 — 2026-07-27

- Electron桌面内核、CHART组合、自然语言长程Agent、SQLite、Checkpoint、目标任务证据反思和资产导出。
